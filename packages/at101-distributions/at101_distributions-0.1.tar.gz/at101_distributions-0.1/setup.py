from setuptools import setup

setup(name='at101_distributions',
      version='0.1',
      description='Gaussian distributions',
      packages=['at101_distributions'],
      author='Alex Terry',
      zip_safe=False)

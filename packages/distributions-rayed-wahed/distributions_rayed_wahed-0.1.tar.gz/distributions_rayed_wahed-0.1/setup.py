from setuptools import setup

setup(name='distributions_rayed_wahed',
      version='0.1',
      description='Gaussian Distributions',
      packages=['distributions_rayed_wahed'],
      zip_safe=False)

from setuptools import setup

setup(name='Exercise_upload_distributions',
      version='0.1',
      description='Gaussian distributions',
      packages=['Exercise_upload_distributions'],
      zip_safe=False)

from setuptools import setup

setup(name='distributions_udacity_kh',
      version='0.1',
      description='Gaussian distributions',
      packages=['distributions_udacity_kh'],
      zip_safe=False)

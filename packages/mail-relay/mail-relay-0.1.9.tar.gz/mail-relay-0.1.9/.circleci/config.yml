version: 2.1
jobs:
  build:

    machine: true
    steps:
      - checkout
      - run:
          name: unit tests
          command: |
            make docker-test

      - run:
          name: lint
          command: |
            make docker-lint

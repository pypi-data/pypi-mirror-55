from setuptools import setup

setup(name='probability_distributions_ms',
      version='0.1',
      description='Gaussian distributions',
      packages=['probability_distributions_ms'],
      zip_safe=False)

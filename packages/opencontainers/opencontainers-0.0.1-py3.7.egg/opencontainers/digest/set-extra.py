        # It might also be a list with an object inside
        #elif isinstance(self.attType, list):
        #    if isinstance(value, list) and value:
        #        innerType = self.attType[0]

                # If we also have an inner structure, update it
        #        if self._is_struct(innerType):
        #            newStruct = innerType()

        #           # TODO what if there are more than one? How to load objects here?
        #            # Load the provided value
        #            newStruct.load(value[0])


from setuptools import setup

setup(name='Gaussian_Binomial_Tool',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['Gaussian_Binomial_Tool'],
      zip_safe=False)

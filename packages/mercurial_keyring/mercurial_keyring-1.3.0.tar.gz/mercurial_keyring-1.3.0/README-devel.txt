
To test in devel virtual environment:

   pip install Mercurial==…
   pip install keyring
   pip install dbus-python

(then install meu and keyring, and possibly other necessary extensions)

dbus-python makes some Linux backends available.
